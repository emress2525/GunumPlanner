name: Build Ankara Avukat Firsat Radari APK

on:
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - run: echo "APK build temporarily paused while v0.3 archive is uploaded"
